#if !defined(POINT_H)
#define POINT_H
class Point
{

};
#endif
/*
POINT_H is a dummy macro to check of any inclusion is present earlier or not.
This is required to prevent multiple inclusions issues.

whatever is within #if..#endif
#pragma once  ==> also works in the same way but its specific to MS compiler

MACRO name should be different for all the header files.
*/
