int main() {
	struct Point {
		int m_x;
		int m_y;
	};
}
/*
'Point' is user defined type (UDT).
'm_x' &'m_y' are called data members.
here, m_ prefexing enables to recognise the variables as data members. This is just a practice and not a rule.

'struct' is keyword of language.
*/